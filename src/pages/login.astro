---
import BaseLayout from '../layouts/BaseLayout.astro';
---
<BaseLayout title="Iniciar sesión · Lukita">
  <div class="relative mx-auto flex min-h-dvh max-w-lg flex-col justify-center px-4 py-10">
    <div class="animate-fade-up mb-10 text-center">
      <img src="/logo-lukita.svg" alt="Lukita" class="mx-auto h-12" />
      <p class="font-display mt-4 text-2xl font-bold tracking-tight">Lukita</p>
      <p class="mt-2 text-sm text-[var(--color-muted-foreground)]">Tus finanzas, claras y simples</p>
    </div>
    <div class="animate-fade-up-delay-1 surface rounded-2xl p-6 shadow-sm">
      <form method="POST" action="/api/auth" class="space-y-4">
        <input type="hidden" name="intent" value="login" />
        <div>
          <label class="mb-1.5 block text-sm font-medium" for="email">Email</label>
          <input id="email" name="email" type="email" required class="flex h-11 w-full rounded-xl border border-[var(--color-border)] bg-[var(--color-background)] px-3 text-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-ring)]" />
        </div>
        <div>
          <label class="mb-1.5 block text-sm font-medium" for="password">Contraseña</label>
          <input id="password" name="password" type="password" minlength="6" required class="flex h-11 w-full rounded-xl border border-[var(--color-border)] bg-[var(--color-background)] px-3 text-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-ring)]" />
        </div>
        <button type="submit" class="brand-gradient h-11 w-full rounded-xl text-sm font-semibold text-[#163038] transition-opacity hover:opacity-90">Entrar</button>
      </form>
      <p class="mt-5 text-center text-sm text-[var(--color-muted-foreground)]">
        ¿No tienes cuenta? <a href="/register" class="font-medium text-[var(--color-primary)]">Regístrate</a>
      </p>
    </div>
  </div>
</BaseLayout>
